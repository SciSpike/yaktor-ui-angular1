(function() {
  
  // Generate a UI model based on a SciSpike generated state matrix.
  
  var fs = require('fs'),
      path = require('path'),
      ansi = require('ansi'),
      cursor = ansi(process.stdout),
      S = require('string'),
  
  parse = {};
  
  parse.fromStateMatrix = function(obj) {
    
    
    
  }
  
  module.exports = parse;
}());